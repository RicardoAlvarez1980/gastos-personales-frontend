import { useState } from 'react';
import { usePeriodos } from './hooks/usePeriodos';
import { useGastos } from './hooks/useGastos';
import { useServicios } from './hooks/useServicios';
import { useAgregarGasto } from './hooks/useAgregarGasto';
import ListadoGastos from './components/gastos/ListadoGastos';
import FormularioGasto from './components/gastos/FormularioGasto';
import Modal from './components/common/Modal';
import { formatearImporte, formatearNombreServicio } from './utils/formateo';
import './styles/v2.css';
import './styles/analisis.css';

const menu = [
  { id: 'inicio', label: 'Resumen anual', icon: '▦' },
  { id: 'gastos', label: 'Gastos mensuales', icon: '▤' },
  { id: 'agregar', label: 'Agregar gasto', icon: '＋' },
  { id: 'analisis', label: 'Análisis', icon: '◔' },
  { id: 'buscar', label: 'Buscar', icon: '⌕' },
];
const mesesNombre = ['Enero','Febrero','Marzo','Abril','Mayo','Junio','Julio','Agosto','Septiembre','Octubre','Noviembre','Diciembre'];

function SelectorAnio({ anios, anio, setAnio, loading }) { return <div className="v2-filters"><div><label htmlFor="year">Año</label><select id="year" value={anio ?? ''} onChange={e => setAnio(Number(e.target.value))} disabled={loading || !anios.length}>{!anios.length && <option value="">Sin datos</option>}{anios.map(item => <option key={item} value={item}>{item}</option>)}</select></div></div>; }
function SelectorMes({ meses, mes, setMes }) { return <div className="v2-filters"><div><label htmlFor="month">Mes</label><select id="month" value={mes ?? ''} onChange={e => setMes(Number(e.target.value))} disabled={!meses.length}>{!meses.length && <option value="">Sin datos</option>}{meses.map(item => <option key={item} value={item}>{mesesNombre[item - 1] ?? item}</option>)}</div></div>; }
function Inicio({ periodos, gastosState, onEliminar, onEditar }) { const { gastos } = gastosState; const total = gastos.reduce((sum, gasto) => sum + Number(gasto.importe || 0), 0); return <section className="v2-dashboard"><div className="v2-welcome"><div><span className="v2-eyebrow">RESUMEN ANUAL</span><h2>Gastos del año</h2><p>Todos los gastos registrados, ordenados por mes y luego por servicio.</p></div><SelectorAnio {...periodos} /></div>{periodos.error && <div className="v2-error">{periodos.error}</div>}<ListadoGastos anual anio={periodos.anio} gastos={gastos} loading={gastosState.loading} error={gastosState.error} onEliminar={onEliminar} onEditar={onEditar} total={total} titulo="GASTOS DEL AÑO" /></section>; }
function GastosMensuales({ periodos, gastosState, onEliminar, onEditar }) { const { gastos } = gastosState; const total = gastos.reduce((sum, gasto) => sum + Number(gasto.importe || 0), 0); return <section className="v2-dashboard"><div className="v2-page-toolbar"><div><span className="v2-eyebrow">DETALLE MENSUAL</span><h2>Gastos de un mes</h2><p>Seleccioná un año y un mes para ver solamente ese período.</p></div><div className="v2-period-filters"><SelectorAnio {...periodos} /><SelectorMes {...periodos} /></div></div><ListadoGastos anio={periodos.anio} mes={periodos.mes} gastos={gastos} loading={gastosState.loading} error={gastosState.error} onEliminar={onEliminar} onEditar={onEditar} total={total} titulo="GASTOS REGISTRADOS" /></section>; }
function Analisis({ periodos, gastosState, servicios }) { const gastos = gastosState.gastos || []; const totalesPorMes = Array.from({ length: 12 }, (_, i) => gastos.filter(g => Number(g.mes) === i + 1).reduce((sum, g) => sum + Number(g.importe || 0), 0)); const mesesConDatos = totalesPorMes.filter(v => v > 0); const totalAnual = totalesPorMes.reduce((sum, v) => sum + v, 0); const promedio = mesesConDatos.length ? totalAnual / mesesConDatos.length : 0; const mayor = mesesConDatos.length ? Math.max(...mesesConDatos) : 0; const menor = mesesConDatos.length ? Math.min(...mesesConDatos) : 0; const totalPorServicio = servicios.map(servicio => ({ ...servicio, total: gastos.filter(g => Number(g.servicio_id) === Number(servicio.id)).reduce((sum, g) => sum + Number(g.importe || 0), 0) })).filter(s => s.total > 0).sort((a,b) => b.total - a.total); const maxMes = Math.max(...totalesPorMes, 0); const limiteInferior = promedio * 0.9; const limiteSuperior = promedio * 1.1; const subtotalSemestre = inicio => totalesPorMes.slice(inicio, inicio + 6).reduce((sum, valor) => sum + valor, 0); return <section className="v2-dashboard"><div className="v2-page-toolbar"><div><span className="v2-eyebrow">ANÁLISIS HISTÓRICO</span><h2>Cómo se distribuyen tus gastos</h2><p>Una lectura del año seleccionado basada únicamente en tus gastos registrados.</p></div><SelectorAnio {...periodos} /></div>{gastosState.loading ? <div className="v2-state">Cargando análisis…</div> : <><div className="v2-analysis-stats"><article><span>Total anual</span><strong>{formatearImporte(totalAnual)}</strong></article><article><span>Promedio mensual</span><strong>{formatearImporte(promedio)}</strong></article><article><span>Mes de mayor gasto</span><strong>{formatearImporte(mayor)}</strong></article><article><span>Mes de menor gasto</span><strong>{formatearImporte(menor)}</strong></article></div><div className="v2-analysis-grid"><section className="v2-analysis-card"><div className="v2-analysis-title"><div><span className="v2-eyebrow">EVOLUCIÓN</span><h3>Gasto por mes</h3></div><strong>{periodos.anio}</strong></div><div className="v2-analysis-legend"><span><i className="above" /> Gasto alto</span><span><i className="average" /> Gasto normal</span><span><i className="below" /> Gasto bajo</span></div><div className="v2-bars">{totalesPorMes.map((valor, i) => { const estado = valor === 0 ? 'empty' : valor > limiteSuperior ? 'above' : valor >= limiteInferior ? 'average' : 'below'; return <div className="v2-bar-item" key={i} title={`${mesesNombre[i]} · Importe: ${formatearImporte(valor)}`}><div className="v2-bar-track"><div className={`v2-bar-fill ${estado}`} style={{ height: maxMes ? `${Math.max((valor / maxMes) * 100, valor ? 3 : 0)}%` : '0%' }} /></div><small>{mesesNombre[i].slice(0,3)}</small></div>; })}</div></section><section className="v2-analysis-card"><div className="v2-analysis-title"><div><span className="v2-eyebrow">SERVICIOS</span><h3>En qué se fue el dinero</h3></div></div>{totalPorServicio.length ? <div className="v2-service-ranking">{totalPorServicio.map((servicio, i) => <div className="v2-service-row" key={servicio.id}><span className="v2-service-rank">{i + 1}</span><span className="v2-service-name">{formatearNombreServicio(servicio.nombre)}</span><strong>{formatearImporte(servicio.total)}</strong></div>)}</div> : <div className="v2-empty">No hay gastos registrados para este año.</div>}</section></div><section className="v2-analysis-card v2-month-comparison"><div className="v2-analysis-title"><div><span className="v2-eyebrow">COMPARACIÓN MENSUAL</span><h3>Comparación por semestre</h3></div></div><div className="v2-analysis-semesters"><div className="v2-semester"><div className="v2-semester-heading"><h3>Primer semestre</h3><strong>Subtotal: {formatearImporte(subtotalSemestre(0))}</strong></div><div className="v2-analysis-months">{totalesPorMes.slice(0, 6).map((valor, i) => valor > 0 && <div key={i}><span>{mesesNombre[i]}</span><strong>{formatearImporte(valor)}</strong></div>)}</div></div><div className="v2-semester"><div className="v2-semester-heading"><h3>Segundo semestre</h3><strong>Subtotal: {formatearImporte(subtotalSemestre(6))}</strong></div><div className="v2-analysis-months">{totalesPorMes.slice(6, 12).map((valor, i) => valor > 0 && <div key={i}><span>{mesesNombre[i + 6]}</span><strong>{formatearImporte(valor)}</strong></div>)}</div></div></div></section></>}</section>; }
function Agregar({ periodos, servicios, mutation }) { return <section className="v2-dashboard"><div className="v2-page-toolbar"><div><span className="v2-eyebrow">NUEVO REGISTRO</span><h2>Agregar gasto</h2><p>Registrá lo que realmente pagaste.</p></div></div>{mutation.error && !mutation.confirmacion && <div className="v2-error">{mutation.error}</div>}<FormularioGasto anios={periodos.anios} servicios={servicios} onGuardar={mutation.guardar} loading={mutation.loading} />{mutation.confirmacion && <Modal title="El gasto ya existe" icon="!" tone="edit" onCancel={mutation.cancelar} onConfirm={mutation.confirmar} confirmLabel="Modificar importe"><div className="v2-modal-context"><strong>{mutation.confirmacion.nuevo?.servicio_nombre || 'Gasto registrado'}</strong><span>{mesesNombre[(Number(mutation.confirmacion.nuevo?.mes) || 1) - 1] || ''} {mutation.confirmacion.nuevo?.año || ''}</span></div><p>Ya existe un gasto para ese año, mes y servicio.</p><div className="v2-delete-amount">Actual: {formatearImporte(mutation.confirmacion.existente.importe)}</div><div className="v2-delete-amount">Nuevo: {formatearImporte(mutation.confirmacion.nuevo.importe)}</div><p className="v2-modal-hint">¿Querés reemplazar el importe actual?</p></Modal>}</section>; }
function Placeholder({ section }) { const item = menu.find(entry => entry.id === section); return <section className="v2-placeholder"><span className="v2-eyebrow">GASTOS PERSONALES · V2</span><h2>{item?.label}</h2><p>Este módulo será conectado en la próxima etapa.</p></section>; }
export default function App() { const [section, setSection] = useState('inicio'); const [gastoGuardado, setGastoGuardado] = useState(null); const periodos = usePeriodos(); const gastosAnuales = useGastos(periodos.anio, 'ANUAL'); const gastosMensuales = useGastos(periodos.anio, periodos.mes); const servicios = useServicios(); const refrescarTodo = async (anioPreferido = periodos.anio, mesPreferido = periodos.mes) => { await periodos.refrescar({ anioPreferido, mesPreferido }); }; const eliminarAnual = async (id) => { const ok = await gastosAnuales.eliminar(id); if (ok) await periodos.refrescar({ anioPreferido: periodos.anio, mesPreferido: periodos.mes }); return ok; }; const eliminarMensual = async (id) => { const ok = await gastosMensuales.eliminar(id); if (ok) await periodos.refrescar({ anioPreferido: periodos.anio, mesPreferido: periodos.mes }); return ok; }; const handleGastoGuardado = async (nuevo) => { await refrescarTodo(nuevo.año, nuevo.mes); setSection('gastos'); setGastoGuardado(nuevo); }; const mutation = useAgregarGasto({ onSuccess: handleGastoGuardado }); const servicioGuardado = gastoGuardado ? servicios.servicios.find(s => Number(s.id) === Number(gastoGuardado.servicio_id)) : null; const cerrarExito = () => setGastoGuardado(null); const irAlListado = async () => { setGastoGuardado(null); await gastosMensuales.recargar(); setSection('gastos'); }; const contenido = section === 'inicio' ? <Inicio periodos={periodos} gastosState={gastosAnuales} onEliminar={eliminarAnual} onEditar={gastosAnuales.editarImporte} /> : section === 'gastos' ? <GastosMensuales periodos={periodos} gastosState={gastosMensuales} onEliminar={eliminarMensual} onEditar={gastosMensuales.editarImporte} /> : section === 'analisis' ? <Analisis periodos={periodos} gastosState={gastosAnuales} servicios={servicios.servicios} /> : section === 'agregar' ? <Agregar periodos={periodos} servicios={servicios.servicios} mutation={mutation} /> : <Placeholder section={section} />; return <div className="v2-shell"><aside className="v2-sidebar"><div className="v2-brand"><span className="v2-brand-mark">$</span><div><strong>Gastos</strong><small>PERSONALES · V2</small></div></div><nav>{menu.map(item => <button key={item.id} className={section === item.id ? 'active' : ''} onClick={() => setSection(item.id)}><span>{item.icon}</span>{item.label}</button>)}</nav><div className="v2-sidebar-footer">Versión 2.0 · base inicial</div></aside><main className="v2-main"><header className="v2-header"><div><span className="v2-eyebrow">CONTROL PERSONAL</span><h1>{menu.find(item => item.id === section)?.label}</h1></div></header>{contenido}</main>{gastoGuardado && <Modal title="Gasto agregado correctamente" icon="✓" tone="success" onCancel={cerrarExito} onConfirm={irAlListado} confirmLabel={`Ver gastos de ${mesesNombre[Number(gastoGuardado.mes) - 1]}`}><div className="v2-modal-context"><strong>{servicioGuardado ? formatearNombreServicio(servicioGuardado.nombre) : 'Gasto registrado'}</strong><span>{mesesNombre[Number(gastoGuardado.mes) - 1]} {gastoGuardado.año}</span></div><div className="v2-success-amount">{formatearImporte(gastoGuardado.importe)}</div><p className="v2-modal-hint">El gasto fue guardado correctamente. Podés revisar todos los gastos de este período.</p></Modal>}</div>; }